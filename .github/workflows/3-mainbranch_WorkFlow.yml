name: MainBranch Workflow

on: 
    push:
        branches:
            - main

jobs:
    first-workflow:
        runs-on: windows-latest

        steps:
            - name: first mainbranch step
              run: |
                echo "Hello World.!"
                echo "Md. Sumon Sarder.!"

